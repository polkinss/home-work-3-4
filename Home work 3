class Human:
     def init(self,name="Human"):
            self.name = name
class Auto:
     def init(self,brand):
            self.brand = brand
            self.passengers = []

     def add_passengers(self, human):
             self.passengers.append(human)

     def print_passengers_name(self):
         if self.passengers != []: print(f"Names of{self.brand}passengers")
            for passengers in self.passengers:

         else:


print(f"There are on passengers in {self.brand}")
nick = Human("Nick")
kate = Human("Kate")
car = Auto("BMW")

car.add_passengers(nick)
car.add_passengers(kate)
car.print_passengers_name()